"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.MAX_SURFACE_SIDE = exports.MAX_SURFACE_PIXELS = void 0;
exports.reserveSurface = reserveSurface;
exports.surfaceBounds = surfaceBounds;
exports.cropSurface = cropSurface;
const bounds_1 = require("../../css/layout/bounds");
const length_percentage_1 = require("../../css/types/length-percentage");
const filter_surface_1 = require("./filter-surface");
// Budget backing stores across all active nested surfaces in one capture.
// Reserve source, filtered output and decoded SVG/PNG rasters conservatively.
// Browser encoder/decoder overhead and the caller's output canvas are separate.
exports.MAX_SURFACE_PIXELS = 16 * 1024 * 1024;
exports.MAX_SURFACE_SIDE = 8192;
function reserveSurface(budget, width, height) {
    const pixels = width * height * 4;
    if (!Number.isFinite(pixels) ||
        width <= 0 ||
        height <= 0 ||
        width > exports.MAX_SURFACE_SIDE ||
        height > exports.MAX_SURFACE_SIDE ||
        budget.pixels + pixels > exports.MAX_SURFACE_PIXELS)
        return 0;
    budget.pixels += pixels;
    return pixels;
}
const expand = (b, margin) => b.add(-margin, -margin, margin * 2, margin * 2);
const union = (a, b) => new bounds_1.Bounds(Math.min(a.left, b.left), Math.min(a.top, b.top), Math.max(a.left + a.width, b.left + b.width) - Math.min(a.left, b.left), Math.max(a.top + a.height, b.top + b.height) - Math.min(a.top, b.top));
// Conservative paint bounds for the untransformed subtree supported by this path.
// Include overflow descendants, text ink padding, box/text shadows and nested filters.
function surfaceBounds(container) {
    const styles = container.styles;
    let bounds = container.bounds;
    for (const shadow of styles.boxShadow) {
        if (!shadow.inset) {
            const spread = Math.max(0, shadow.spread.number) + 3 * shadow.blur.number;
            bounds = union(bounds, expand(container.bounds.add(shadow.offsetX.number, shadow.offsetY.number, 0, 0), spread));
        }
    }
    for (const text of container.textNodes) {
        for (const part of text.textBounds) {
            const ink = expand(part.bounds, 2 * (0, length_percentage_1.getAbsoluteValue)(styles.fontSize, container.bounds.height) + styles.webkitTextStrokeWidth);
            bounds = union(bounds, ink);
            for (const shadow of styles.textShadow) {
                bounds = union(bounds, expand(ink.add(shadow.offsetX.number, shadow.offsetY.number, 0, 0), 3 * shadow.blur.number));
            }
        }
    }
    for (const child of container.elements) {
        if (child.styles.isVisible())
            bounds = union(bounds, surfaceBounds(child));
    }
    const filter = (0, filter_surface_1.parseSimpleFilter)(styles.filter);
    return expand(bounds, filter ? (0, filter_surface_1.filterOutset)(filter) : 0);
}
function cropSurface(bounds, viewport, margin, scale) {
    // Pixels just outside the destination still contribute through blur/shadow.
    const area = expand(viewport, margin);
    const left = Math.floor(Math.max(bounds.left, area.left) * scale) / scale;
    const top = Math.floor(Math.max(bounds.top, area.top) * scale) / scale;
    const right = Math.ceil(Math.min(bounds.left + bounds.width, area.left + area.width) * scale) / scale;
    const bottom = Math.ceil(Math.min(bounds.top + bounds.height, area.top + area.height) * scale) / scale;
    return new bounds_1.Bounds(left, top, Math.max(0, right - left), Math.max(0, bottom - top));
}
